# vs-plugin-sdk

`vs-plugin-sdk` is the author-facing helper crate for native `vs` plugins.

## Responsibilities

- re-export shared plugin types
- provide a typed contract wrapper for native plugins
- keep plugin authors away from internal application crates
